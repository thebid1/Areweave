import { StdFee } from "@cosmjs/amino/build/signdoc";
import { IBasePendingTx, KyveSigning, PendingSignedTx, PendingTx } from "../../../signing";
export default class KyveBaseMsg extends KyveSigning {
    transfer(recipient: string, amount: string, options?: {
        fee?: StdFee | "auto" | number;
        memo?: string;
    }): PendingTx<PendingSignedTx>;
    multiTransfer(recipient: string[], amount: string, options?: {
        fee?: StdFee | "auto" | number;
        memo?: string;
    }): PendingTx<PendingSignedTx>;
    txsAll(txs: PendingSignedTx[] | IBasePendingTx[], options?: {
        fee?: StdFee | "auto" | number;
        memo?: string;
    }): PendingTx<PendingSignedTx>;
}
