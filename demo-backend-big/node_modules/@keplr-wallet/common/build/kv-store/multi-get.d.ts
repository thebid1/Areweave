import { KVStore, MultiGet } from "./interface";
export declare class WrapMultiGetKVStore implements MultiGet {
    protected readonly kvStore: KVStore;
    constructor(kvStore: KVStore);
    multiGet(keys: string[]): Promise<{
        [key: string]: any;
    }>;
}
